#include "TextLabel.h"

TextLabel::TextLabel(glm::vec3 givenOrigin, glm::vec3 givenColor, float givenWidth, std::string givenText){

	origin = givenOrigin;
	color = givenColor;
	width = givenWidth;
	text = givenText;
}
